// make the box disapear when the user clicks it
const redbox = document.getElementById("red-box")

const disappearBox = () => {
    red-box.remove()
}
red-box.addEventListener("click", disappearBox)
